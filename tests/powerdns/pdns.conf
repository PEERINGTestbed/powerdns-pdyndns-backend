daemon=no
setgid=pdns
setuid=pdns
local-address=127.0.0.1

launch=pipe
pipe-timeout=2000
pipe-abi-version=3
pipe-regex=dyndns.example.net$
pipe-command=pdyndns.py --config tests/pdyndns.json

distributor-threads=1
out-of-zone-additional-processing=no

